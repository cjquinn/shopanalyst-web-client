import colours from '../colours';
import font from '../font';

const Input = {
    default: {
        background: colours.faintBeige,
        outline: colours.green,
        placeholder: {
            colour: colours.beige
        }
    },
    inverse: {
        background: colours.white
    },
    large: {
        fontSize: font.large
    }
};

export default Input;
