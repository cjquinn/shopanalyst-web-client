import colours from '../colours';
import font from '../font';

const Link = {
    default: {
        fontSize: font.medium
    },
    primary: {
        colour: colours.green
    },
    danger: {
        colour: colours.red
    }
};

export default Link;
