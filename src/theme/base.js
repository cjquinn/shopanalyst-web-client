import colours from './colours';
import font from './font';

const base = {
    background: colours.lightBeige,
    colour: colours.grey,
    fontFamily: font.family,
    fontSize: font.base,
    spacing: {
        single: '10px',
        double: '20px'
    }
};

export default base;
